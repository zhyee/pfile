// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pfile", "for pfile support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pfile", "enable pfile support", "no");

if (PHP_PFILE != "no") {
	EXTENSION("pfile", "pfile.c");
}

