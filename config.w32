// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("rockfile", "for rockfile support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("rockfile", "enable rockfile support", "no");

if (PHP_ROCKFILE != "no") {
	EXTENSION("rockfile", "rockfile.c");
}

